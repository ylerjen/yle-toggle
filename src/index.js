// import "@babel/polyfill";  // should be uncommented if older browser support is needed
import { defineCustomElement } from './scripts/component';

console.log('bundle parsed');

defineCustomElement();